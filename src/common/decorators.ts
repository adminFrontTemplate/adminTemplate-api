// import { applyDecorators, Query } from '@nestjs/common';
// import { ApiQuery as SwaggerApiQuery, ApiQueryOptions } from '@nestjs/swagger';

// export function CustomApiQuery(
//   options: ApiQueryOptions,
// ): ReturnType<typeof applyDecorators> {
//   return applyDecorators(
//     Query(options.name, { required: options.required }),
//     SwaggerApiQuery(options.name, options),
//   );
// }
